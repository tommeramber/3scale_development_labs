<section class="plans" id="plans">
    <div class="container">
        <br/>
        {% if provider.signups_enabled? %}
            <a href="{{ urls.signup }}" class="navbar-brand">Register and be approved for a plan</a>
        {% endif %}
    </div>
</section>
